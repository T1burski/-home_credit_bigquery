# -home_credit_bigquery

### This repo has the objective to host the code that manages all layers of the data used in the Home Credit Model project. dbt core is used to manage the data in a Google BigQuery DW. 
